import './header.scss'


const Header = () => {
    return (
        <header className={'header'}>
            <div className="container">
                <h1 className={'header__title'}>Контакты</h1>
            </div>
        </header>
    );
};

export default Header;